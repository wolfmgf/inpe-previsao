/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nenhuma configuração personalizada é necessária para nossa aplicação no momento.
  // Deixamos o objeto vazio para usar os padrões do Next.js.
};

export default nextConfig;
